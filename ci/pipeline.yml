resources:
- name: flight-school
  type: git
  source:
    uri: https://github.com/watawuwu/flight-school
    branch: feature/test
    check_every: 5s

jobs:
- name: test-app
  plan:
  - get: flight-school
    trigger: true
  - task: tests
    file: flight-school/build.yml
